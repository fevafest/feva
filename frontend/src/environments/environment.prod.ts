// Update these two values to match your actual Render backend URL once it's
// deployed (Render → your service → the URL shown at the top of the page),
// then rebuild and redeploy the frontend. Example shown below is a placeholder.
export const environment = {
  production: true,
  apiUrl: 'https://feva-fest-api.onrender.com/api',
  fileBaseUrl: 'https://feva-fest-api.onrender.com',
};
